# Run viz

    Run "embeding_viz.py".
    Packages to install: "pip install plotly dash dash_bootstrap_components".
    
    The currently used csv file "embedding_data_clean_v1.csv" is uploaded to shodan shared folder under the path "/plotdata/cleaned_data/", see "jsonl_to_csv.py" to learn more about the structure of this csv file.

    
